{
  "name": "payout-process",
  "version": "1.0.0",
  "description": "A React TypeScript frontend with Python FastAPI backend for PDF analysis using OpenAI Vision API",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && uvicorn main:app --reload",
    "build": "cd frontend && npm run build",
    "install:all": "npm install --prefix frontend && pip install -r backend/requirements.txt"
  },
  "keywords": ["react", "typescript", "fastapi", "openai", "pdf", "vercel"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}